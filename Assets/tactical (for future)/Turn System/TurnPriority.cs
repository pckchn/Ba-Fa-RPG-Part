using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TurnPriority : MonoBehaviour
{
    // Start is called before the first frame update
    public float priority;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
